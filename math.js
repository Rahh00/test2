module.exports = {
  add: (a, b) => a + b,
  subtract: (a, b) => a - b,
  multiply: (a, b) => a * b,
  divide: (a, b) => a / b,
};


// adding feature 3
let feature3 = "adding feature 3
//adding feature 1
let feature1 = " adding feature 1"

